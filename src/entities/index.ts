import { Category } from "./category.entity";
import { Location } from "./location.entity";
import { User } from "./user.entity";

// export "entities" array to be used with "MikroORM.init"
export default [ User, Location, Category ];
